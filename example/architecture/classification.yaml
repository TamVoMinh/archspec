# Classification dimensions for the shopping-system example.
# Both `area` and `criticality` are closed vocabularies — `sda check` validates values.
dimensions:
  area:
    values: [storefront, fulfillment, payments, platform]
  criticality:
    values: [core, supporting, generic]

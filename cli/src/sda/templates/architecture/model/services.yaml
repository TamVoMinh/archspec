services:
  # Add your services here. Each service should include:
  #   type:          api | postgres | redis | worker | queue | external | frontend | other
  #   depends_on:    list of other service names
  #   owner:         email of the domain owner
  #   last_reviewed: date this entry was last reviewed (YYYY-MM-DD)
  #
  # Example:
  # api-gateway:
  #   type: api
  #   depends_on: [auth, billing]
  #   owner: architect@company.com
  #   last_reviewed: 2026-04-06

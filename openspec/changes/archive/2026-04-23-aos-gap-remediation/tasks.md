## 1. Repository Structure

- [x] 1.1 Create `schemas/` directory at repository root
- [x] 1.2 Create `scripts/` directory at repository root
- [x] 1.3 Verify `architecture/inbox/`, `architecture/adr/`, and `architecture/model/` directories exist with placeholder files

## 2. YAML Schema Definitions (Gap 2)

- [x] 2.1 Create `schemas/problem.schema.yaml` with required fields: `id`, `title`, `source`, `type`, `created_at`, `services`, `symptoms`; optional fields: `constraints`, `tags`, `status`
- [x] 2.2 Create `schemas/service.schema.yaml` with optional fields: `type`, `depends_on`, `owner`, `last_reviewed`, `deprecated`
- [x] 2.3 Create `schemas/adr.schema.yaml` covering the `## Metadata` block fields: `status`, `date`, `superseded_by`, `links`
- [x] 2.4 Validate schemas against existing sample files using `yamale` locally
- [x] 2.5 Add CI step to `.github/workflows/` (or equivalent) that runs `yamale` against inbox and model files

## 3. ADR Lifecycle Enforcement (Gap 3)

- [x] 3.1 Update ADR template (`architecture/adr/TEMPLATE.md` or equivalent) to include the `## Metadata` block with `status`, `date`, `superseded_by`, and `links` fields
- [x] 3.2 Write `scripts/check-adr-lifecycle.py` that reads all ADR files, verifies presence of `status` field, validates state machine transitions, and checks that `superseded` ADRs have a non-null `superseded_by`
- [x] 3.3 Write `scripts/check-adr-services.py` that verifies no `accepted` ADR references a `deprecated: true` service from `services.yaml`
- [x] 3.4 Add both scripts as a blocking CI step
- [x] 3.5 Backfill `status: accepted` (or appropriate status) into all existing ADR files

## 4. Ownership Model (Gap 4)

- [x] 4.1 Create `OWNERS.yaml` at repository root with `roles.architecture_lead`, `roles.contributors`, `triage_policy`, and `domain_ownership` sections
- [x] 4.2 Fill in actual names, emails, and domain assignments for all current team members
- [x] 4.3 Create or update `.github/CODEOWNERS` to map `architecture/model/` and `schemas/` to the architecture lead, and domain-specific ADR paths to domain owners
- [x] 4.4 Add `owners.schema.yaml` to `schemas/` and validate `OWNERS.yaml` against it in CI
- [x] 4.5 Verify all services in `services.yaml` have a corresponding `domain_ownership` entry in `OWNERS.yaml`

## 5. Knowledge Graph Index (Gap 1)

- [x] 5.1 Write `scripts/aos-index.py` that reads all `PROB-*.yaml` files from `architecture/inbox/` and extracts `id`, `services`, and links
- [x] 5.2 Extend `scripts/aos-index.py` to read all ADR Markdown files and extract `## Metadata` links, affected services, and status
- [x] 5.3 Have the script emit `architecture/index.yaml` with a `graph:` top-level key containing entries keyed by problem ID, ADR ID, and service name
- [x] 5.4 Add an auto-generated header comment to `index.yaml` output: `# AUTO-GENERATED by scripts/aos-index.py — do not edit manually`
- [x] 5.5 Add CI step that runs `aos-index.py` and validates that the committed `index.yaml` matches the generated output (warn, non-blocking)
- [x] 5.6 Commit initial `architecture/index.yaml` generated from existing files

## 6. Staleness Detection (Gap 6)

- [x] 6.1 Add `last_reviewed` and `owner` fields to all existing service entries in `architecture/model/services.yaml`
- [x] 6.2 Write `scripts/check-staleness.py` that reads `services.yaml`, compares each `last_reviewed` date against today, and flags services older than 180 days
- [x] 6.3 Handle missing `last_reviewed` fields with a distinct `MISSING` warning message
- [x] 6.4 Add the script as a non-blocking CI warning step (default exit code `0`)
- [x] 6.5 Document how to switch to blocking mode in `scripts/check-staleness.py` comments

## 7. Input Capture Workflow (Gap 5)

- [x] 7.1 Update `schemas/problem.schema.yaml` to allow `status: draft` with empty `services` and `symptoms` lists (conditional validation)
- [x] 7.2 Create a draft problem file template at `architecture/inbox/PROB-TEMPLATE.yaml` to guide contributors
- [x] 7.3 Write `scripts/aos-capture.py` (or shell script) that accepts a title argument, determines the next PROB ID, and scaffolds a draft problem YAML file
- [x] 7.4 Document the capture workflow (per-source instructions: Slack, Jira, meetings, ad hoc) in `architecture/README.md` or equivalent
- [x] 7.5 Add a stale-draft check to `scripts/check-staleness.py` that flags draft problems older than `triage_sla_hours` from `OWNERS.yaml`

## 8. Multi-Architect Governance (Gap 7)

- [x] 8.1 Create the four ADR PR labels in the repository settings: `adr/proposed`, `adr/accepted`, `adr/rejected`, `adr/contested`
- [x] 8.2 Document the label lifecycle and conflict resolution policy in a new `OWNERS.yaml` section under `governance`
- [x] 8.3 Write or reference the PR review requirement matrix (new inbox, ADR proposed, ADR accepted, model change, schema change) in `OWNERS.yaml` under `triage_policy`
- [x] 8.4 Configure branch protection rules to enforce minimum approvers per path (model and schema changes require arch lead)
- [x] 8.5 Create a PR template at `.github/pull_request_template.md` that prompts contributors to set the correct ADR label and verify required reviewers

## 9. Verification

- [x] 9.1 Run all CI scripts locally against the repository and confirm zero blocking failures
- [x] 9.2 Confirm `architecture/index.yaml` is generated and contains entries for all existing problems, ADRs, and services
- [x] 9.3 Confirm all existing ADR files have a valid `status` field
- [x] 9.4 Confirm all existing services in `services.yaml` have `last_reviewed` and `owner` fields
- [x] 9.5 Open a test PR and verify `CODEOWNERS` triggers the correct reviewer requests
- [x] 9.6 Run `python scripts/check-staleness.py` and confirm output matches expected stale/missing entries
- [x] 9.6 Run `python scripts/check-staleness.py` and confirm output matches expected stale/missing entries
